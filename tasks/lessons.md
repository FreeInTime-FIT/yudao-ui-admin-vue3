# lessons

- 2026-03-18：用户明确指出 3D 可点击区域在“右下角那个变电房对象”时，不能只凭模型里看起来像的节点名（如 `变电房.glb`）直接绑定；必须核对完整节点层级和对象规模，优先绑定真正承载该可视对象的根节点（本次是 `biandian.glb`）。
- 2026-03-18：glTF 原始 JSON 里的节点名不一定等于 Three 运行时射线命中的对象名；像 `biandian.glb` 这类名称在运行时会被规范化成 `biandianglb`。做 3D 点击命中时，名称匹配逻辑必须兼容这种规范化差异。
